version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign

  exclusions:
    paths:
      - .claude
      - .github
      - frontend
      - node_modules
