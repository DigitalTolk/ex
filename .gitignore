# OS
.DS_Store
Thumbs.db

# IDE
/.idea/
/.vscode/
*.swp
*.swo
*~

# Environment
/.env
/.env.local
/.env.*.local
/AGENTS.md
/CLAUDE.md
/SPEC.md
/.claude/scheduled_tasks.lock

# Dev-only local Cliffy user seeder — never committed (creates a local
# login for manual testing; not part of the server build).
/cmd/cliffylocaluser/

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
/.gocache/
/vendor/

# Frontend (lives at the repo root)
# Ignore dist *contents* (build artifacts) but keep the directory
# itself tracked so frontend.go's //go:embed has something to match
# on a fresh checkout. The dist/.gitignore file is the placeholder.
/dist/*
!/dist/.gitignore
/.vite/
*.local

# Build
/ex
/migrate
/bin/
/tmp/
node_modules/

# Coverage
/coverage/
/coverage-browser/
coverage.out
coverage.html
/.vitest-attachments/
__screenshots__/

# Machine-local dev launcher with CLIFFY_* env (secret sourced from CliffHub .env).
/run-cliffy-dev.sh
